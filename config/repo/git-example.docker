# You really want to keep this to a single RUN statement. Each additional RUN adds another layer to your
# container.

RUN git clone --recurse-submodules -j8 -b master https://github.com/YoeDistro/yoe-distro.git yoe

